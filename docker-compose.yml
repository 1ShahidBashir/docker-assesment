
version: "3.9"

services:
  web:
    build: .
    container_name: flask_demo_dev
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
    command: python app.py
